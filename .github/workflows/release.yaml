name: Release APWorld
on:
    push:
        tags:
            - "**"
jobs:
    release:
        uses: Eijebong/ap-actions/.github/workflows/release-apworld.yml@main
        with:
            apworld-path: super_paper_mario
            ap-version: "main"
            python-version: "3.11"
